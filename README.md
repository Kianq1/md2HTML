# Markdown → HTML Converter (with CSS Templates)

A Python GUI application that converts Markdown (`.md`) files into simple HTML documents.  
Users can select a Markdown file, choose from multiple CSS templates(templates are so simple , if you want you can put your own css in code), set a custom HTML title, and save the output. The app includes a simple GUI and an option to automatically preview the result in a browser(only works on some devices).

## ✅ Features
- Select a Markdown file with a built in file picker✅
- Choose from built‑in CSS templates:
  - Dark Mode✅
  - Light Mode✅
  - Minimal✅
  - Fancy✅
- Convert Markdown to HTML with embedded CSS✅
- auto‑open the HTML in your default browser(only works on some devices)✅
- GUI✅


## 🛠️ Technologies Used
- **Tkinter** (GUI)
- **markdown2** (Markdown conversion)


  
## 📦 Installation
Make sure you have Python 3 installed. Then install the required package:

```bash
pip install markdown2
pip install tkinter
```

and run it with :
```bash
python md2html.py
```
